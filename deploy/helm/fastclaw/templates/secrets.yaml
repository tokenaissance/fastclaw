apiVersion: v1
kind: Secret
metadata:
  name: {{ include "fastclaw.fullname" . }}-admin
  labels:
    {{- include "fastclaw.labels" . | nindent 4 }}
type: Opaque
stringData:
  token: {{ include "fastclaw.adminToken" . | quote }}
  dsn: {{ include "fastclaw.dsn" . | quote }}
