Bundled skills are intentionally empty. FastClaw acts as a pure agent runtime;
products bring their own skills via FASTCLAW_HOME/skills/ or per-agent skills/.
Drop a skill folder in here and rebuild if you want the binary to ship with one.
